"""Conversation app configuration."""

from django.apps import AppConfig


class ConversationConfig(AppConfig):
    """Conversation app configuration."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "conversation"
